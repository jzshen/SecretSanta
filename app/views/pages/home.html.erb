<% if logged_in? %>
  <div class="row">
  <!--
    <aside class="col-md-4">
      <section class="user_info">
        <%= current_user.first_name %>
      </section>
      <section class="stats">
        <p>Stats</p>
      </section>
      <section class="micropost_form">
        <p>Form<p>
      </section>
    </aside>
  -->
    <div class="center jumbotron">
      <h1>Welcome <%= current_user.first_name %></h1>
      	<h2>Looking good today!</h2>
        <%= link_to 'Join a group!', groups_path, class: "btn btn-lg btn-primary" %>
    </div>
  </div>

<!-- NOT LOGGED IN -->
<% else %>
  <div class="center jumbotron">
  	<h1>JenShen's Secret Santa App</h1>
  	<h2>Welcome Scarberians, this is just for you.</h2>
    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
   </div>
<% end %>

<div class="tree"><i class="fa fa-tree fa-5x"></i></tree>
<%#= image_tag("tree.png", class: "saturate") %>

